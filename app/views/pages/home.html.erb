

<div class="row justify-content-md-center">
  <div class="col-md-6">

<% if @current_user.present? %>

<h2>Home Page</h2>
<p> Hi  <%= @current_user.name %>, what would you like to to today? </P>
<ul>
    <li> <%= link_to "New Journey", trips_new_path %> </li>
    <li> <%= link_to "Add Funds", transactions_new_path %> </li>
    <li> <%= link_to "View transactions", transactions_index_path %> </li>
    <li> <%= link_to "View past trips", trips_index_path %> </li>
</ul>
<% else %>
 <h3>
 Please sign-in or sign-up.
</h3>
<div class="row justify-content-md-center">
    <%= link_to 'Sign up', users_new_path, class: "btn btn-outline-primary" %>
    <%= link_to 'Sign in', login_path, class: "btn btn-outline-primary" %>
    </div>
<% end %>


<svg width="500" height="500">
    // // Draw Edges
    // for edge in @edges do
    //    from = @stops[edge.from_id]
    //    from_x = from.location_x
    //    from_y = from.location_y
    //    to = @stops[edge.to_id]
    //    to_x = to.location_x
    //    to_y = to.location_y
    //    
    //    svg_draw_line([from_x, from_y], [to_x, to_y])
    // 

    // Draw Stops
    <% @stops.each do |stop| %>
    <circle cx="<%= (500/25)*stop.location_x %>" cy="<%= (500/25)*stop.location_y %>" r="5" stroke="black" stroke-width="2" fill="white" />
    <text x="<%= (500/25)*stop.location_x+10 %>" y="<%= (500/25)*stop.location_y %>"><%= stop.name %></text>
    <%  end %> 
</svg>


 </div>
</div>